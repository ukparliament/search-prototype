<% unless witnesses.blank? %>
  <div class="about-item">
    <div class="content-heading-small" id="witnesses">Witnesses</div>
    <% witnesses.each do |witness| %>
      <%= search_link(witness) %><br/>
    <% end %>
  </div>
<% end %>