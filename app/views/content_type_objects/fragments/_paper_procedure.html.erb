<% unless procedure.blank? %>
  <dt>
    Procedure
  </dt>
  <dd>
    <%= search_link(procedure) %>
  </dd>
  <% unless approval_days.blank? || approval_days[:value]&.to_i.zero? %>
    <dd>
      <%= approval_days[:value] %> days scrutiny period
    </dd>
  <% end %>
<% end %>