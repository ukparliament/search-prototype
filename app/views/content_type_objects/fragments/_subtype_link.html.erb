<% if object.has_link? %>
  <dt>Link</dt>
  <dd>
    <%= link_to "View this #{object_display_name(object.subtype_or_type)} on #{ URI.parse(object.display_link[:value]).host}", object.display_link[:value] %>
  </dd>
<% end %>