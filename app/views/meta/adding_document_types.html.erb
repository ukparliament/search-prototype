<%= content_tag( 'h2', 'Before we start' ) %>
<ol>
	<%= content_tag( 'li', 'Anya decides which document type we want to look at next.' ) %>
	<%= content_tag( 'li', 'Jianhan provides example XML from SOLR of a search result for that that document type.' ) %>
</ol>

<%= content_tag( 'h2', 'Adding the mock data' ) %>
<ol>
	<%= content_tag( 'li', "Made a new folder at #{link_to( 'https://github.com/ukparliament/search-mock/tree/main/lib/xml/results', 'https://github.com/ukparliament/search-mock/tree/main/lib/xml/results' )} for the document type".html_safe ) %>
	<%= content_tag( 'li', "In that folder, make a new module like #{link_to( 'https://github.com/ukparliament/search-mock/blob/main/lib/xml/results/written-statements/solr.rb', 'https://github.com/ukparliament/search-mock/blob/main/lib/xml/results/written-statements/solr.rb' )}.".html_safe ) %>
	<%= content_tag( 'li', 'Call the module *CONTENT_TYPE*_SOLR_RESULTS_XML.' ) %>
	<%= content_tag( 'li', 'Call the method *content_type*_results_xml and paste in the SOLR XML from Jianhan.' ) %>
	<%= content_tag( 'li', "In the #{link_to( 'search controller', 'https://github.com/ukparliament/search-mock/blob/main/app/controllers/search_controller.rb' ) }, add a require for the new file and an include for the new module.".html_safe )%>
	<%= content_tag( 'li', "In the #{link_to( 'case statement', 'https://github.com/ukparliament/search-mock/blob/main/app/controllers/search_controller.rb#L26' ) }, add a new when for the document type and set the xml to *content_type*_results_xml.".html_safe ) %>
	<%= content_tag( 'li', "Add a link to the new document type in #{link_to( 'results list on the form page', 'https://github.com/ukparliament/search-mock/blob/main/app/views/search/index.html.erb#L6' )}.".html_safe ) %>
</ol>
	
<%= content_tag( 'h2', 'Tweaking the prototype' ) %>
<ol>
	<%= content_tag( 'li', "To get more data for the result set in general, #{link_to( 'edit the parse results method', 'https://github.com/ukparliament/search-prototype/blob/main/app/controllers/search_controller.rb#L65' )} to store the variable via an XPath and assign the variable to the @result_set object.".html_safe ) %>
	<%= content_tag( 'li', "Every new variable you assign to the result set object will need to be included as an attr_accessor in the #{link_to( 'ResultSet model', 'https://github.com/ukparliament/search-prototype/blob/main/app/models/result_set.rb')}.".html_safe ) %>
	<%= content_tag( 'li', "Edit the #{link_to( 'results template', 'https://github.com/ukparliament/search-prototype/blob/main/app/views/search/results.html.erb' )} to bung it on the page.".html_safe ) %>
	<%= content_tag( 'li', "To get more data for a given result, #{link_to( 'edit the parse result item method', 'https://github.com/ukparliament/search-prototype/blob/main/app/controllers/search_controller.rb#L97' )} to store the variable via an XPath and assign the variable to the result object.".html_safe ) %>
	<%= content_tag( 'li', "Every new variable you assign to the result object will need to be included as an attr_accessor in the #{link_to( 'Result model', 'https://github.com/ukparliament/search-prototype/blob/main/app/models/result.rb')}.".html_safe ) %>
	<%= content_tag( 'li', "Edit the #{link_to( 'result partial', 'https://github.com/ukparliament/search-prototype/blob/main/app/views/search/_result.html.erb' )} to bung it on the page.".html_safe ) %>
</ol>

<%= content_tag( 'h2', 'Making it look decent' ) %>
<ol>
	<%= content_tag( 'li', "Edit the #{link_to( 'CSS', 'https://github.com/ukparliament/ontologies/blob/master/meta/assets/trrb.css' ) }.".html_safe ) %>
</ol>