<div class="content-prelim-box">
  <div class="content-prelim-inner">
    <div class="content-prelim-text" id="prelim-text">
      A <%= link_to(object.object_name, search_path(filter: object.type)) %> tabled on
      <strong><%= format_date(object.date_of_question) %></strong>
      <% unless object.department.blank? %>
        in the <%= ses_object_link(object.legislature) %>,
      <% end %>
      by <%= ses_object_link(object.tabling_member) %> (<%= ses_object_link(object.tabling_member_party) %>)
      to the <%= ses_object_link(object.department) %>.
      It has been withdrawn.
    </div>
  </div>
</div>

