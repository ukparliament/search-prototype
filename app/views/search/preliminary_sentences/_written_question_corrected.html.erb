<div class="content-prelim-box">
  <div class="content-prelim-inner">
    <div class="content-prelim-text" id="prelim-text">
      A <%= object_display_name_link(object.object_name) %>
      asked by <%= search_link(object.tabling_member) %> (<%= search_link(object.tabling_member_party) %>)
      on <strong><%= format_date(object.date_of_question) %></strong>,
      in the <%= search_link(object.legislature) %>.
      <% unless object.date_for_answer.blank? %>
        It was due for answer
        on <strong><%= format_date(object.date_for_answer) %></strong>
        <% unless object.question_type.blank? %>
          (<%= object.question_type[:value]&.downcase %>)
        <% end %>
        .
      <% end %>
      It was answered by <%= search_link(object.answering_member) %>
      (<%= search_link(object.answering_member_party) %>)
      <% if object.department.blank? %>
        on <strong><%= format_date(object.date_of_answer) %></strong>.
      <% else %>
        on <strong><%= format_date(object.date_of_answer) %></strong>
        on behalf of the <%= search_link(object.department) %>.
      <% end %>
      <% unless object.asked_to_reply_author.blank? %>
        <%= search_link(object.asked_to_reply_author) %> was asked to reply.
      <% end %>
    </div>
  </div>
</div>