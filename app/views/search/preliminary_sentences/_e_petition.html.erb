<div class="content-prelim-box">
  <div class="content-prelim-inner">
    <div class="content-prelim-text" id="prelim-text">
      An <%= link_to(object.object_name, search_path(filter: object.type)) %> published on
      <strong><%= format_date(object.date) %></strong>.
      It has been signed by <strong><%= object.number_of_signatures[:value] %></strong> people.
      <% if object.closed? %>
        It is now closed.
      <% end %>
    </div>
  </div>
</div>