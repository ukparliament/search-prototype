<div class="content-prelim-box">
  <div class="content-prelim-inner">
    <div class="content-prelim-text">
      An <%= object_link("#{object.object_name.titleize}", '/') %> published
      by <%= object_link("#{object.published_by}", '/') %>
      on <strong><%= object.published_on&.strftime(ApplicationHelper::DATE_DISPLAY_FORMAT) %></strong>.
      <% if object.updated_on %>
        It was updated on <strong><%= object.updated_on&.strftime(ApplicationHelper::DATE_DISPLAY_FORMAT) %></strong>.
      <% end %>
    </div>
  </div>
</div>

